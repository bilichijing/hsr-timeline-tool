# 崩坏：星穹铁道 排轴工具 · 开发文档

> 项目代号：`hsr-timeline-tool`
> 文档版本：1.0
> 最后更新：2026-08-11

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [数据源说明](#3-数据源说明)
4. [项目结构](#4-项目结构)
5. [开发流程](#5-开发流程)
6. [核心设计](#6-核心设计)
7. [缓存策略](#7-缓存策略)
8. [打包发布](#8-打包发布)
9. [开发规范](#9-开发规范)
10. [freesr-data.json 角色面板导入](#10-freesr-datajson-角色面板导入)
11. [真实角色技能模块](#11-真实角色技能模块)

---

## 1. 项目概述

### 1.1 项目目标

为本《崩坏：星穹铁道》玩家提供一款本地运行的排轴与伤害计算工具，支持：

- 队伍配置（角色 / 光锥 / 遗器 / 词条）
- 回合制排轴编辑（基于行动值 AV 系统）
- 战斗模拟与伤害计算
- 结果分析（DPS、伤害占比、轮次效率）
- 工程存档与分享

### 1.2 运行形态

- **本地桌面应用**，打包为 `.exe` 单文件分发
- 无需服务器资源，无 CORS 限制
- 数据从 `hsr.nanoka.cc` 拉取，本地缓存

### 1.3 与参考项目（wuwa-afyg-tool）的关系

- **参考**：数据层架构思路（manifest 版本管理、列表/详情分离、transform 提纯）
- **不参考**：业务逻辑（实时动作 vs 回合制，完全不同）
- **独立项目**：不共享代码，不共享数据源

### 1.4 关键差异认知

| 维度 | 鸣潮（参考项目） | 星铁（本项目） |
|---|---|---|
| 战斗模型 | 实时动作 | **回合制** |
| 时间轴 | 连续秒数 | **离散行动值（AV）** |
| 速度机制 | 无 | **核心机制**（10000/速度=AV） |
| 资源 | 共鸣能量 | **战技点（SP）**，上限 5 |
| Buff 时效 | 持续秒数 | 持续回合数 |
| 关键指标 | DPS | **轮次伤害 / 0T / 行动效率** |

---

## 2. 技术栈

| 层 | 库 | 版本 | 用途 |
|---|---|---|---|
| GUI 框架 | `PySide6` | ≥6.7 | 主界面框架 |
| HTTP 客户端 | `httpx` | ≥0.27 | 异步拉取 nanoka 数据 |
| 数据模型 | `pydantic` | ≥2.5 | 数据校验与类型定义 |
| 磁盘缓存 | `diskcache` | ≥5.6 | SQLite 后端缓存 |
| 图标处理 | `Pillow` | ≥10.0 | webp/png 图标下载与转换 |
| 日志 | `loguru` | ≥0.7 | 结构化日志 |
| 图表 | `QtCharts`（PySide6 内置） | - | 伤害占比、DPS 曲线 |
| 图标库 | `qtawesome` | ≥1.3 | Font Awesome / MDI 图标 |
| 打包 | `PyInstaller` | ≥6.0 | 生成 .exe |
| 测试 | `pytest` | ≥8.0 | 单元测试 |
| 代码风格 | `ruff` | ≥0.5 | lint + format |
| 包管理 | `uv` | ≥0.3 | 依赖管理 |

### 安装命令

```bash
uv init hsr-timeline-tool
cd hsr-timeline-tool
uv add PySide6 httpx pydantic diskcache Pillow loguru qtawesome
uv add --dev pytest pytest-asyncio ruff pyinstaller
```

---

## 3. 数据源说明

### 3.1 数据源地址

```
基址：https://static.nanoka.cc
星铁数据根：https://static.nanoka.cc/hsr
资源根：    https://static.nanoka.cc/assets/hsr
```

### 3.2 Manifest 结构（实测）

`GET https://static.nanoka.cc/manifest.json`

返回多游戏统一 manifest，本项目只关心 `hsr` 字段：

```json
{
  "gi": { ... },          // 原神（忽略）
  "hsr": {
    "latest": "4.4.55",
    "available": ["4.4", "4.4.51", "4.4.52", "4.4.53", "4.4.54", "4.4.55"],
    "live": "4.4",
    "new": {
      "character": [1512, 1513],
      "lightcone": [21066, 22008, 23063, 23064],
      "relicset": [],
      "monster": [2022072, 2022082, ...],
      "item": [140630, 140631, ...]
    }
  },
  "ww": { ... },          // 鸣潮（忽略）
  "zzz": { ... },         // 绝区零（忽略）
  "nte": { ... }
}
```

字段说明：
- `latest`：最新版本号（用于拼接数据路径）
- `available`：所有可用版本（可用于版本切换）
- `live`：正式服版本
- `new`：该版本新增内容（用于"新角色"标记）

### 3.3 路径模式

#### 列表（轻量，含英文名与基础属性）

```
GET https://static.nanoka.cc/hsr/{version}/{entity}.json
```

| entity | 内容 | 示例大小 |
|---|---|---|
| `character` | 角色列表 | ~54 KB |
| `lightcone` | 光锥列表 | ~145 KB |
| `relicset` | 遗器套装列表 | ~92 KB |
| `monster` | 怪物列表 | ~456 KB |

> ⚠️ 列表只含英文名（`en` 字段），中文名在详情 JSON 里。

#### 详情（重型，含完整技能/星魂/属性）

```
GET https://static.nanoka.cc/hsr/{version}/zh/{entity}/{id}.json
```

示例：`https://static.nanoka.cc/hsr/4.4/zh/character/1001.json`（约 84 KB）

### 3.4 列表字段（实测）

#### character.json

```json
{
  "1001": {
    "release": 1682460000,
    "icon": "mar7th",
    "rank": "CombatPowerAvatarRarityType4",
    "baseType": "Knight",
    "damageType": "Ice",
    "en": "March 7th",
    "enhance": []
  }
}
```

#### lightcone.json

```json
{
  "20000": {
    "rank": "CombatPowerLightconeRarity3",
    "baseType": "Rogue",
    "en": "Arrows",
    "atk": 318,
    "desc": "..."
  }
}
```

#### relicset.json

```json
{
  "101": {
    "icon": "SpriteOutput/ItemIcon/71000.png",
    "en": "Passerby of Wandering Cloud",
    "set": { "2": { "en": "..." } }
  }
}
```

### 3.5 详情字段（实测）

`zh/character/{id}.json` 顶层字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `name` | string | 中文名 |
| `desc` | string | 角色描述 |
| `chara_info` | object | 阵营、配音等元信息 |
| `rarity` | string | 稀有度枚举（见映射表） |
| `sp_need` | number | 战技点消耗 |
| `base_type` | string | 命途枚举（见映射表） |
| `damage_type` | string | 属性枚举（见映射表） |
| `ranks` | object | 星魂（E1-E6） |
| `skills` | object | 技能（普攻/战技/终结技/天赋/秘技） |
| `skill_trees` | object | 行迹树（含倍率参数） |
| `enhanced` | object | 强化技能（如三月七强化普攻） |
| `memosprite` | object | 忆灵数据（2.x 新机制） |
| `stats` | object | 各等级基础属性表 |
| `relics` | array | 推荐遗器 |
| `lightcones` | array | 推荐光锥 |
| `teams` | array | 推荐队伍 |
| `skin` | object | 皮肤 |

### 3.6 枚举映射表

#### rarity（稀有度）

| nanoka 值 | 数字 | 说明 |
|---|---|---|
| `CombatPowerAvatarRarityType4` | 4 | 4 星角色 |
| `CombatPowerAvatarRarityType5` | 5 | 5 星角色 |
| `CombatPowerLightconeRarity3` | 3 | 3星光锥 |
| `CombatPowerLightconeRarity4` | 4 | 4星光锥 |
| `CombatPowerLightconeRarity5` | 5 | 5星光锥 |

#### base_type（命途）

| nanoka 值 | 中文名 | 英文名 |
|---|---|---|
| `Knight` | 存护 | Preservation |
| `Rogue` | 毁灭 | Destruction |
| `Mage` | 智识 | Erudition |
| `Warlock` | 虚无 | Nihility |
| `Shaman` | 丰饶 | Abundance |
| `Priest` | 同谐 | Harmony |
| `Warrior` | 巡猎 | Hunt |
| `Memory` | 记忆 | Remembrance |
| `Elation` | 欢愉 | Elation |

#### damage_type（属性）

| nanoka 值 | 中文名 | 英文名 |
|---|---|---|
| `Physical` | 物理 | Physical |
| `Fire` | 火 | Fire |
| `Ice` | 冰 | Ice |
| `Thunder` | 雷 | Lightning |
| `Wind` | 风 | Wind |
| `Quantum` | 量子 | Quantum |
| `Imaginary` | 虚数 | Imaginary |

### 3.7 图标资源路径

图标 URL 需根据 nanoka 的 `icon` 字段拼接，具体路径模式待阶段 1 实测确认。初步推测：

```
https://static.nanoka.cc/assets/hsr/{icon_path}
```

> 📌 **待办**：阶段 1 需实测图标 URL 格式，补充到此节。

---

## 4. 项目结构

```
hsr-timeline-tool/
├── docs/                        # 开发文档
│   ├── 开发文档.md             # 本文档（整体规划）
│   └── 技术规范.md             # 技术规范与开发约定
├── src/
│   ├── api/                     # 数据层（对应 wuwa 的 lib/api/）
│   │   ├── __init__.py
│   │   ├── consts.py            # NANOKA_BASE、版本管理、枚举映射
│   │   ├── client.py            # httpx 客户端 + diskcache
│   │   ├── models.py            # pydantic 模型（对应 types.ts）
│   │   └── transforms.py        # 数据提纯（对应 utils.ts）
│   ├── core/                    # 业务核心（不依赖 GUI）
│   │   ├── __init__.py
│   │   ├── stats.py             # 属性系统（HP/ATK/DEF/SPD/暴击/击破）
│   │   ├── av_system.py         # 行动值系统（速度→AV、行动队列）
│   │   ├── skill.py             # 技能解析（倍率提取、类型识别）
│   │   ├── buff.py              # Buff 系统（时效、叠加、触发）
│   │   ├── damage.py            # 伤害公式（基础伤害、击破伤害）
│   │   ├── sp.py                # 战技点管理
│   │   └── simulator.py         # 战斗模拟引擎
│   ├── ui/                      # PySide6 界面
│   │   ├── __init__.py
│   │   ├── main_window.py       # 主窗口
│   │   ├── theme.py             # QSS 主题
│   │   ├── widgets/             # 自定义控件
│   │   │   ├── character_picker.py
│   │   │   ├── lightcone_picker.py
│   │   │   ├── relic_config.py
│   │   │   ├── team_editor.py
│   │   │   ├── timeline_view.py    # 排轴时间轴
│   │   │   ├── result_chart.py     # QtCharts 图表
│   │   │   └── buff_modal.py
│   │   └── assets/              # 本地图标、字体、qss
│   ├── data/                    # 持久化数据
│   │   ├── project.py           # 工程存档（JSON 读写）
│   │   └── settings.py          # 用户设置
│   └── app.py                   # 应用入口
├── tests/                       # 单元测试
│   ├── test_api.py
│   ├── test_stats.py
│   ├── test_av_system.py
│   ├── test_damage.py
│   └── test_buff.py
├── cache/                       # diskcache 数据（运行时生成）
├── main.py                      # 启动入口
├── pyproject.toml
└── README.md
```

### 模块依赖关系

```
main.py
  └─ src/app.py
       ├─ src/ui/*        （依赖 core 和 api）
       ├─ src/core/*      （纯逻辑，不依赖 UI，可独立测试）
       └─ src/api/*       （数据获取，不依赖 core 和 UI）
```

**关键原则**：`core/` 模块必须可独立测试，不导入任何 `PySide6` 代码。

---

## 5. 开发流程

### 阶段 0：数据勘探（已完成 ✅）

**成果**：本文档第 3 节。

### 阶段 1：API 数据层

**目标**：建立 nanoka 数据获取与本地缓存能力。

#### 任务清单

- [ ] `api/consts.py`：常量定义、枚举映射表、版本管理函数
- [ ] `api/client.py`：httpx 异步客户端 + diskcache 封装
- [ ] `api/models.py`：pydantic 模型（Character、Lightcone、RelicSet、Monster 及其详情类型）
- [ ] `api/transforms.py`：nanoka 原始数据 → 精简模型
- [ ] 图标 URL 格式实测与下载器
- [ ] 单元测试：`tests/test_api.py`

#### 关键设计

**版本管理**（简化版，无需 wuwa 的双路径策略）：

```python
import diskcache
import httpx

cache = diskcache.Cache('./cache')

def get_latest_version() -> str:
    cached = cache.get('hsr_version')
    if cached:
        return cached
    manifest = httpx.get("https://static.nanoka.cc/manifest.json").json()
    version = manifest['hsr']['latest']
    cache.set('hsr_version', version, expire=3600)  # 1 小时
    return version
```

**中文名获取**（列表只有英文，需两级查询）：

```python
async def get_character_zh_names() -> dict[int, str]:
    """返回 {id: 中文名} 映射，首次启动全量拉取并缓存"""
    cached = cache.get('character_zh_names')
    if cached:
        return cached
    version = get_latest_version()
    list_data = await fetch_json(f"/hsr/{version}/character.json")
    names = {}
    for char_id in list_data:
        detail = await fetch_json(f"/hsr/{version}/zh/character/{char_id}.json")
        names[char_id] = detail['name']
    cache.set('character_zh_names', names, expire=86400)  # 1 天
    return names
```

#### 验收标准

- 能拉取角色列表并显示中文名
- 能拉取单个角色详情并解析出：属性、命途、技能倍率、星魂描述
- 缓存命中：第二次请求 < 50ms
- `pytest tests/test_api.py` 全部通过

---

### 阶段 2：业务核心建模（最复杂）

**目标**：实现星铁回合制战斗的所有计算逻辑，**用单元测试驱动，不依赖 GUI**。

#### 任务清单

- [ ] `core/stats.py`：属性系统
- [ ] `core/av_system.py`：行动值系统
- [ ] `core/skill.py`：技能解析
- [ ] `core/buff.py`：Buff 系统
- [ ] `core/damage.py`：伤害公式
- [ ] `core/sp.py`：战技点管理
- [ ] 单元测试：全部 `tests/test_*.py`

#### 验收标准

- 给定角色属性 + buff，伤害计算与游戏内一致（误差 < 1%）
- 4 个角色不同速度，行动顺序与游戏内一致
- `pytest tests/` 覆盖率 ≥ 80%

详见 [核心设计](#6-核心设计) 章节。

---

### 阶段 3：界面骨架与配置页

**目标**：搭建主窗口框架，完成队伍配置界面。

#### 任务清单

- [ ] `ui/main_window.py`：主窗口、菜单栏、侧栏、Tab 容器
- [ ] `ui/theme.py`：QSS 主题（亮/暗）
- [ ] `ui/widgets/character_picker.py`：角色选择器（搜索 + 筛选）
- [ ] `ui/widgets/lightcone_picker.py`：光锥选择器（按命途过滤）
- [ ] `ui/widgets/relic_config.py`：遗器配置（4件套 + 2件套 + 主词条 + 副词条）
- [ ] `ui/widgets/team_editor.py`：队伍编成（4 槽 + 怪物配置）
- [ ] `data/project.py`：工程存档 JSON 读写
- [ ] `data/settings.py`：用户设置

#### 主窗口布局

```
┌──────────────────────────────────────────────┐
│ 菜单栏：工程 | 设置 | 帮助                   │
├──────────┬───────────────────────────────────┤
│ 工程侧栏  │ 主区域（Tab 切换）               │
│ - 队伍1   │ ┌─配置─┬─排轴─┬─结果─┐          │
│ - 队伍2   │ │                                 │          │
│ + 新建    │ │  当前 Tab 内容                  │          │
│          │ │                                 │          │
│          │ └─────────────────────────────────┘          │
└──────────┴───────────────────────────────────┘
```

#### 验收标准

- 能完整配置一个 4 人队伍（角色 + 光锥 + 遗器 + 词条）
- 工程能保存到 JSON 并重新加载
- 界面响应流畅，无卡顿

---

### 阶段 4：排轴与战斗模拟（项目灵魂）

**目标**：实现回合制排轴编辑与战斗模拟引擎。

#### 任务清单

- [ ] `ui/widgets/timeline_view.py`：AV 时间轴控件
- [ ] `core/simulator.py`：战斗模拟引擎
- [ ] `ui/widgets/buff_modal.py`：Buff 配置弹窗
- [ ] 操作序列序列化（存档/读档）

#### 时间轴控件设计

```
AV      0    50   100  150  200  250
角色A   ├───┼───┤────┼───┤
角色B   ├───────┤────┼───┤
角色C   ├───────────┤───┤
怪物     ├─────────┤────┤
         普攻  战技 终结技
```

- **横轴是行动值（AV）**，不是秒
- 每行一个角色/怪物，显示行动队列
- 操作块可拖拽排序

#### 模拟引擎主循环

```
输入：队伍配置 + 操作序列
输出：每个行动的伤害明细 + AV 时间轴

模拟循环：
  while 累计总行动值 < max_av (默认 300):
    1. 取行动队列下一个行动者（AV 最小者）
    2. 累计总行动值 += 行动者当前 AV
    3. 执行其操作（普攻/战技/终结技/怪物行动/阿哈时刻）
    4. 结算伤害、buff 生效/失效
    5. 更新 SP、能量、韧性
    6. 推进行动队列（所有单位 AV 减去本次消耗值，行动者重置）
```

**战斗结束条件**：以累计总行动值判定（默认 300），而非最大回合数。
每条行动日志（`ActionLog`）记录 `total_av` 字段，表示该行动结束时的累计总行动值。

#### 关键难点

- **Buff 时效结算**：回合制下"持续 2 回合"的精确定义
- **速度变化**：遗器套装、角色天赋会在行动中改变速度，需动态重排队列
- **击破状态**：韧性归零后进入击破状态，伤害加成不同
- **忆灵**：召唤物有独立行动条

#### 验收标准

- 能完整模拟一场总行动值达 300 的战斗
- 行动顺序与游戏内一致
- buff 叠加/失效逻辑正确
- 行动日志中每条记录都包含总行动值

---

### 阶段 5：结果分析与打包

**目标**：结果展示与可分发打包。

#### 任务清单

- [ ] `ui/widgets/result_chart.py`：QtCharts 图表
- [ ] 数据分析表格（每行动伤害明细）
- [ ] 对比模式（两个配置并排）
- [ ] PyInstaller 打包配置
- [ ] 首次启动图标下载逻辑

#### 验收标准

- 能展示伤害占比饼图、轮次伤害柱状图
- `pyinstaller` 打包出可运行的 .exe
- 在干净 Windows 环境下双击即可运行

---

## 6. 核心设计

### 6.1 行动值（AV）系统

星铁的核心机制。速度决定行动顺序。

#### 基础公式

```
行动值（AV） = 10000 / 速度
```

- 每个角色/怪物初始 AV = 10000 / 自身速度
- AV 最小的先行动
- 行动后 AV 重置为 10000 / 速度，其余角色 AV 减去已消耗值

#### 推条 / 拉条

- **推条**（延后）：AV += X * AV
- **拉条**（提前）：AV -= X * AV（下限 0）
其中X为行动延后/行动提前比例

#### 速度变化时的 AV 调整

速度变化时（如遗器套装、天赋触发），剩余 AV 按以下公式换算：

```
新行动值 = 原行动值 × 新基础行动值 / 原基础行动值 - 新基础行动值 × (提前% - 延后%)
```

其中：
- 基础行动值 = 10000 / 速度
- `提前%` / `延后%` 为本次速度变化同时附带的推拉条比例（无则为 0）

#### 怪物初始 AV

怪物初始 AV 也是 `10000 / 速度`，与角色同规则入队。

#### 实现要点

```python
from dataclasses import dataclass

@dataclass
class ActionEntry:
    unit_id: int
    speed: float
    current_av: float  # 距离下次行动的 AV

class ActionQueue:
    def __init__(self):
        self.entries: list[ActionEntry] = []

    def next_actor(self) -> ActionEntry:
        """返回 AV 最小的行动者"""
        return min(self.entries, key=lambda e: e.current_av)

    def advance(self, av_consumed: float):
        """推进时间，所有单位 AV 减少"""
        for e in self.entries:
            e.current_av -= av_consumed
        # 行动者重置
        actor = self.next_actor()
        actor.current_av = 10000.0 / actor.speed

    def apply_push(self, unit_id: int, av_delta_rate: float):
        """推条（正数延后）或拉条（负数提前）"""
        for e in self.entries:
            if e.unit_id == unit_id:
                e.current_av = max(0, e.current_av * (1 + av_delta_rate))
```

---

### 6.2 战技点（SP）管理

- 队伍共享，上限 5，初始 3
- 普攻 +1，战技 -1
- 某些技能可回复或消耗额外 SP

```python
class SkillPoint:
    MAX = 5
    INITIAL = 3

    def __init__(self):
        self.current = self.INITIAL

    def consume(self, amount: int = 1) -> bool:
        if self.current < amount:
            return False  # SP 不足
        self.current -= amount
        return True

    def recover(self, amount: int = 1):
        self.current = min(self.MAX, self.current + amount)
```

---

### 6.3 Buff 系统

#### 时效类型

| 类型 | 说明 | 失效时机 |
|---|---|---|
| `PERMANENT` | 永久 | 战斗结束 |
| `TURNS_SELF_START` | 持续 N 回合（自身回合开始） | 自身回合开始时 -1 |
| `TURNS_SELF_END` | 持续 N 回合（自身回合结束） | 自身回合结束时 -1 |
| `NEXT_ATTACK` | 下次攻击后 | 攻击命中后 |
| `ONCE` | 一次性触发 | 触发后立即失效 |

**TURNS_SELF_END 特殊规则**：若角色在自身回合内获得该类型 buff，则获得 buff 的这个回合结束时，对应 buff 的持续时间**不会减少**（即本次回合结束不扣减，从下一回合才开始扣减）。这意味着 buff 的"持续 N 回合"是从获得后的下一个自身回合开始算的。

实现要点：buff 记录 `applied_in_self_turn` 标志，回合内获得时置 True，首次 `tick_turn_end` 时跳过扣减并清除标志。

#### 叠加规则

| 规则 | 说明 |
|---|---|
| `NO_STACK_SAME_NAME` | 同名 buff 不叠加，刷新时效 |
| `NO_STACK_SAME_SOURCE` | 同源 buff 不叠加（如同角色两个技能） |
| `STACK_LIMIT_N` | 最多叠 N 层 |

#### 数据结构

```python
from enum import Enum
from dataclasses import dataclass, field

class BuffDuration(Enum):
    PERMANENT = "permanent"
    TURNS_SELF_START = "turns_self_start"
    TURNS_SELF_END = "turns_self_end"
    NEXT_ATTACK = "next_attack"
    ONCE = "once"

@dataclass
class Buff:
    id: str
    name: str
    stat: str              # 影响的属性（atk_pct, dmg_bonus, etc.）
    value: float           # 数值（百分比已 /100）
    duration_type: BuffDuration
    duration_count: int    # 回合数
    source_unit: int       # 来源角色 ID
    stack_rule: str = "no_stack_same_name"
    current_stacks: int = 1
```

---

### 6.4 伤害公式

星铁伤害分为 7 类：**常规伤害、击破伤害、超击破伤害、持续伤害、欢愉伤害、真实伤害**。其中真实伤害不使用抗性系数与防御系数。

**暴击判定**：常规伤害与欢愉伤害采用显式暴击判定——每个伤害段使用固定种子独立随机流掷骰（`rng_seed` 可复现），暴击率钳制到 `[0,1]`；击破/超击破/持续/真实伤害不暴击。暴击区 = `1 + 暴击伤害`。

---

#### 6.4.1 抗性系数

```
抗性系数 = 1 - 抗性
抗性范围：-100% ≤ 抗性 ≤ 90%
```

- 抗性由"基础抗性 - 减抗"得出，最终值钳制在 [-100%, 90%] 区间
- 弱点属性默认抗性 0%，非弱点属性默认抗性 20%

---

#### 6.4.2 防御系数

**角色攻击敌人时**（敌人等级超过 100 时按 100 计算；扑满须改为 `1.5 × 等级 + 30`）：

```
防御系数 = (角色等级 + 20) / [(角色等级 + 20) + (敌人等级 + 20) × (1 - 减防% - 无视防御%)]
```

**敌人攻击角色时**：

```
防御系数 = (10 × 敌人等级 + 200) / (10 × 敌人等级 + 200 + 角色防御)
```

减防与无视防御为加算关系，共同削减敌人防御占比。

---

#### 6.4.3 常规伤害

```
最终伤害 = 基础值 × 特殊独立乘区
         × (1 + 增伤%) × (1 + 易伤%) × (1 - 虚弱%)
         × (1 - 减伤1%) × (1 - 减伤2%) × (1 - 减伤3%) × ...
         × 抗性系数 × 防御系数 × 暴击区
```

各乘区说明：

| 乘区 | 说明 |
|---|---|
| **基础值** | `攻击力 × 倍率`，攻击力可替换为生命值/防御力等（视技能而定） |
| **特殊独立乘区** | 如最终伤害加成等极为特殊的效果，独立乘算 |
| **增伤** | 描述中带有"我方造成的伤害提升 X%"的效果（属性增伤、通用增伤均属此区） |
| **易伤** | 描述中带有"目标受到的伤害提升 X%"的效果 |
| **虚弱** | 描述中带有"虚弱"的特殊效果，独立乘区 |
| **减伤** | 描述中带有"目标受到的伤害降低 X%"的效果 |
| **暴击区** | `暴击率 × 暴击伤害 + (1 - 暴击率)`，不暴击时为 1 |

减伤规则：
- **多个减伤为乘算**：例如两个 50% 减伤 ≈ 一个 75% 减伤（0.5 × 0.5 = 0.25）
- **所有减伤乘算后不低于 1%**（下限保护）
- **敌方未被弱点击破时，必定带有 10% 减伤**（击破后此减伤消失）

---

#### 6.4.4 击破伤害

角色击破弱点时，造成的击破伤害：

```
击破伤害 = 击破基础值 × 属性倍率 × 特殊独立乘区
         × (1 + 易伤%)
         × (1 + 击破特攻%) × (1 + 击破增伤%)
         × (1 - 减伤1%) × (1 - 减伤2%) × ...
         × 抗性系数 × 防御系数
```

击破基础值与属性倍率：

```
击破基础值 = 3767.55 × (韧性上限 / 40 + 0.5)
```

| 属性 | 属性倍率 |
|---|---|
| 火、物理 | 2.0 |
| 风 | 1.5 |
| 冰、雷 | 1.0 |
| 量子、虚数 | 0.5 |

关键规则：
- **击破伤害不受常规增伤和虚弱影响**（无增伤区、无虚弱区）
- 但有专门的**击破特攻**和**击破增伤**两个独立乘区
- 击破伤害仍受易伤、减伤、抗性、防御影响

---

#### 6.4.5 超击破伤害

超击破伤害需要通过**角色技能效果**触发，而非自动产生。

**触发条件**（由技能描述决定）：
- 一般情况：攻击**处于弱点击破状态**的敌方目标时触发
- 特殊情况：某些技能效果允许在未击破状态也触发（如大丽花战技"结界持续期间，敌方目标未处于弱点击破状态时承受的削韧值也能够转化为超击破伤害"）

**触发示例**（大丽花天赋）：
> 【共舞者】攻击处于弱点击破状态的敌方目标后，会将本次攻击的削韧值转化为 1 次 60% 的超击破伤害。

**超击破倍率**由触发源（天赋/光锥/技能效果）提供，不同来源倍率不同。

```
超击破伤害 = 超击破基础值 × 超击破倍率 × 特殊独立乘区
           × (1 + 易伤%)
           × (1 + 击破特攻%) × (1 + 击破增伤%)
           × (1 - 减伤1%) × (1 - 减伤2%) × ...
           × 抗性系数 × 防御系数
```

超击破基础值：

```
超击破基础值 = 3767.55 × 实际削韧值 / 10
```

- 与普通击破伤害相同的乘区结构（不受常规增伤和虚弱）
- 基础值取决于**实际削韧值**（本次攻击削减的韧性），而非韧性上限

---

#### 6.4.6 持续伤害

角色造成的持续伤害（DoT，如触电、灼烧、裂伤等）：

```
持续伤害 = 基础值 × 特殊独立乘区
         × (1 + 增伤%) × (1 + 易伤%) × (1 - 虚弱%)
         × (1 - 减伤1%) × (1 - 减伤2%) × ...
         × 抗性系数 × 防御系数 × 持续伤害暴击区
```

与常规伤害的唯一区别：**暴击区被替换为持续伤害暴击区**。
- 持续伤害默认不能暴击（持续伤害暴击区 = 1）
- 只有极少数效果能提供持续伤害暴击区（如黑天鹅命途等）

---

#### 6.4.7 欢愉伤害

欢愉伤害是新版本引入的特殊伤害类型，拥有独立的乘区结构：

```
欢愉伤害 = 欢愉倍率 × 基础值 × 特殊独立乘区
         × (1 + 欢愉度%) × (1 + 增笑%)
         × (笑点区 或 好活当赏区)
         × (1 + 易伤%)
         × (1 - 减伤1%) × (1 - 减伤2%) × ...
         × 抗性系数 × 防御系数 × 暴击区
```

各乘区说明：

| 乘区 | 说明 |
|---|---|
| **欢愉倍率** | 技能/效果提供的欢愉伤害倍率 |
| **基础值** | 与角色等级相关，80 级角色固定为 `7535.107` |
| **特殊独立乘区** | 如最终伤害加成等极为特殊的效果，独立乘算 |
| **欢愉度** | 欢愉伤害专属加成乘区（类似常规增伤但独立） |
| **增笑** | 欢愉伤害专属加成乘区，与欢愉度相互独立乘算 |
| **笑点区 / 好活当赏区** | 见下方公式，二者择一使用 |
| **易伤** | 与常规伤害共用 |
| **减伤** | 与常规伤害共用（乘算，下限 1%） |
| **抗性系数** | 与常规伤害共用 |
| **防御系数** | 与常规伤害共用 |
| **暴击区** | 与常规伤害一致：`暴击率 × 暴击伤害 + (1 - 暴击率)`，不暴击时为 1 |

关键规则：
- **欢愉伤害不受常规增伤和虚弱影响**（无增伤区、无虚弱区）
- **欢愉伤害可以暴击**，暴击区计算与常规伤害一致
- 欢愉伤害有专属的**欢愉度**和**增笑**两个独立乘区

**笑点区 / 好活当赏区**：

```
笑点区 = 1 + 笑点 × 5 / (笑点 + 240)
好活当赏区 = 1 + 好活当赏 × 5 / (好活当赏 + 240)
```

二者计算结构相同，区别在于使用的属性值不同：

| 触发场景 | 使用乘区 |
|---|---|
| **欢愉技**造成的欢愉伤害 | 笑点区（使用"笑点"属性） |
| **其余**欢愉伤害 | 好活当赏区（使用"好活当赏"属性） |

---

#### 6.4.7a 欢愉系统机制详解

欢愉系统涉及 4 个独立属性 + 阿哈特殊行动机制，是星铁 2.x 之后引入的复杂系统。

**属性分类**：

| 属性 | 类型 | 来源 | 默认值 |
|---|---|---|---|
| **欢愉度** | 角色面板属性 | 光锥效果、遗器套装效果、角色技能 buff | 0 |
| **增笑** | 角色面板属性 | 光锥效果、遗器套装效果、角色技能 buff | 0 |
| **笑点** | 战斗中动态累积的资源 | 战斗开始/阿哈时刻结束/角色技能效果 | 战斗开始时获得 |
| **好活当赏** | 特殊 buff（TURNS_SELF_END） | 战斗开始/阿哈时刻结束/角色技能效果 | 战斗开始时获得 20 点 |

**笑点机制**：
- 战斗开始时获得等同于**我方欢愉角色数量**的笑点
- 阿哈时刻结束后获得等同于**我方欢愉角色数量**的笑点
- 角色技能效果可获得额外笑点
- 阿哈行动触发阿哈时刻时**清空笑点**
- 当我方获得笑点时，行动条上出现阿哈

**好活当赏机制**：
- 特殊 buff，时效类型为 `TURNS_SELF_END`，默认持续 2 回合
- **非欢愉命途角色无法获得好活当赏**
- 各个好活当赏 buff 的持续时间**独立计算**（不是合并为一个 buff）
- 计算伤害时，计入的好活当赏点数 = 角色身上当前还在持续时间内的所有好活当赏 buff 的点数累加值
- **好活当赏点数无上限**
- 战斗开始时获得 20 点
- 每次阿哈时刻结束后，所有欢愉角色获得等同于**本次阿哈时刻笑点数量**的好活当赏

**阿哈机制**：
- 当我方获得笑点时，行动条上出现阿哈
- 阿哈速度计算公式：
  ```
  阿哈速度 = 80
           + 我方速度最高欢愉角色速度 / 5
           + 我方速度第二高欢愉角色速度 / 10
           + 我方速度第三高欢愉角色速度 / 20
           + 我方速度第四高欢愉角色速度 / 50
  ```
- 阿哈行动时触发**阿哈时刻**：
  1. 解除我方所有欢愉角色的控制类负面状态
  2. 各个欢愉角色按照其欢愉技的**参演编号顺序**释放欢愉技
  3. 清空笑点
  4. 阿哈时刻结束后，所有欢愉角色获得好活当赏（点数=本次笑点数量）+ 笑点（数量=我方欢愉角色数量）

**欢愉伤害限定**：
- 目前**仅有欢愉命途角色**能够造成欢愉伤害
- 欢愉倍率的字段读取方式仍待勘探（nanoka 数据结构未知）

---

#### 6.4.8 真实伤害

真实伤害是部分星魂/技能提供的特殊伤害类型（如缇宝 E1）：

```

真实伤害 = 传入的基础伤害值

```

关键规则：

- **无视防御**：不使用防御系数。
- **无视抗性**：不使用抗性系数。
- **不会暴击**：不进入暴击区。
- **不吃任何增伤**：增伤、易伤、虚弱、击破增伤、欢愉度等乘区全部不参与。
- 真实伤害仍参与战斗总伤害统计，并在伤害明细中标记为“真实伤害”。

#### 6.4.9 乘区汇总表

| 乘区 | 常规 | 击破 | 超击破 | 持续 | 欢愉 | 真实伤害 |
|---|---|---|---|---|---|---|
| 基础值（攻击力×倍率） | ✅ | ❌（击破基础值） | ❌（超击破基础值） | ✅ | ❌（等级固定值） | ❌（由效果直接传入） |
| 特殊独立乘区 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 增伤 | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ |
| 易伤 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 虚弱 | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ |
| 减伤（乘算） | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 抗性系数 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 防御系数 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 暴击区 | ✅ | ❌ | ❌ | ❌（持续伤害暴击区） | ✅ | ❌ |
| 击破特攻 | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ |
| 击破增伤 | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ |
| 欢愉度 | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 增笑 | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 笑点区 / 好活当赏区 | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |

---

### 6.5 击破机制

- 每个敌人有**韧性值**（toughness）
- 攻击弱点击破韧性：削韧值 = 基础削韧值 × (1 + 弱点击破效率)
- 韧性归零 → 进入**击破状态**：
  - 触发一次性**击破伤害**（公式见 6.4.4）
  - **取消未击破时的 10% 减伤**
  - **怪物自己行动后恢复韧性**（满血恢复，恢复时不会再次触发击破伤害）

**击破恢复细节**：
- 恢复时机：怪物自身的下次行动结束后
- 恢复量：满血恢复（`current_toughness = max_toughness`）
- 恢复时**不会**再次触发击破伤害
- 恢复后 `is_broken` 置 False，重新进入"未击破 10% 减伤"状态

```python
@dataclass
class EnemyState:
    max_toughness: float
    current_toughness: float
    weakness_elements: list[str]   # 弱点属性列表
    is_broken: bool = False

    def reduce_toughness(self, amount: float, element: str) -> bool:
        """返回是否触发击破"""
        if element not in self.weakness_elements:
            return False
        self.current_toughness = max(0, self.current_toughness - amount)
        if self.current_toughness == 0 and not self.is_broken:
            self.is_broken = True
            return True  # 触发击破伤害
        return False

    def get_damage_reduction(self) -> float:
        """返回当前减伤系数（用于伤害公式中的减伤乘区）"""
        if not self.is_broken:
            return 0.10  # 未击破时 10% 减伤
        return 0.0       # 击破后无减伤
```

---

## 7. 缓存策略

### 三级缓存（简化版）

| 层级 | 机制 | TTL | 用途 |
|---|---|---|---|
| 内存缓存 | Python 模块级变量 | 进程生命周期 | sonata 等共享数据 |
| 磁盘缓存 | `diskcache`（SQLite） | 见下表 | 列表/详情数据 |
| 图标文件 | 本地文件系统 | 永久 | 首次下载后复用 |

### diskcache TTL 策略

| 数据 | TTL | 理由 |
|---|---|---|
| manifest 版本号 | 1 小时 | 版本更新频率低 |
| 列表数据（character.json 等） | 10 分钟 | 对应 wuwa 的 s-maxage=600 |
| 详情数据 | 1 小时 | 详情几乎不变 |
| 中文名映射表 | 1 天 | 变化极少 |
| 图标文件 | 永久 | 不变，手动更新 |

### 实现示例

```python
import diskcache
import httpx

cache = diskcache.Cache('./cache')

async def fetch_json(url: str, expire: int = 600) -> dict:
    cached = cache.get(url)
    if cached is not None:
        return cached
    async with httpx.AsyncClient() as client:
        r = await client.get(url)
        r.raise_for_status()
        data = r.json()
    cache.set(url, data, expire=expire)
    return data
```

---

## 8. 打包发布

### PyInstaller 配置

```bash
pyinstaller --onefile --windowed \
  --name "HSR排轴工具" \
  --add-data "src/ui/assets;ui/assets" \
  --add-data "src/ui/qss;qss" \
  --icon "src/ui/assets/icon.ico" \
  main.py
```

### 首次启动逻辑

```
1. 检查用户目录是否存在图标缓存
2. 若无 → 显示进度条 → 批量下载图标到 %APPDATA%/HSR排轴工具/icons/
3. 检查 manifest 版本 → 若有新版 → 提示更新数据
4. 启动主界面
```

### 版本更新检查

```python
def check_data_update() -> bool:
    """检查 nanoka 是否有新版本数据"""
    manifest = httpx.get("https://static.nanoka.cc/manifest.json").json()
    latest = manifest['hsr']['latest']
    local = settings.get('data_version')
    return latest != local
```

---

## 9. 开发规范

### 9.1 代码风格

- 使用 `ruff` 进行 lint 和 format
- 行宽 100 字符
- 类型注解必须（pydantic 模型 + 函数签名）
- 中文注释（除变量名/函数名用英文）

### 9.2 模块边界

- **`api/`**：只负责数据获取，不导入 `core/` 或 `ui/`
- **`core/`**：纯逻辑，**禁止导入 `PySide6`**，可被 `tests/` 直接测试
- **`ui/`**：界面层，可调用 `core/` 和 `api/`
- **`data/`**：持久化，不依赖 `ui/`

### 9.3 测试要求

- `core/` 模块必须有对应测试
- 伤害公式测试用游戏内实测数据校准
- AV 系统测试覆盖：推条、拉条、速度变化、行动顺序
- `pytest tests/` 必须全部通过才能合并

### 9.4 提交规范

```
<type>: <description>

type:
  feat     新功能
  fix      修复
  refactor 重构
  test     测试
  docs     文档
  chore    杂项
```

### 9.5 分支策略

- `main`：稳定版本
- `dev`：开发主干
- `feat/*`：功能分支
- `fix/*`：修复分支

---

## 10. freesr-data.json 角色面板导入

freesr-data.json 是常用的《崩坏：星穹铁道》角色配置导出格式，包含角色（avatars）、遗器（relics）、光锥（lightcones）三块。UI 配置页"导入 freesr-data.json"按钮可导入并计算最终面板。

### 10.1 格式说明摘要

**avatars**（`{avatar_id: {...}}`，data 为空则跳过）：
- `data.rank`：星魂等级（只记录，星魂效果待实现）
- `data.skills`：技能等级映射——`{char_id}001`=普攻、`{char_id}002`=战技、`{char_id}003`=终结技、`{char_id}004`=天赋（freesr id = nanoka id 中间插 0，如 `1504001` ↔ `150401`）；秘技/额外能力/行迹固定 1
- `level`/`promotion`：固定 80/6
- `sp_max`：能量上限；`sp_value`：进战斗初始能量（经 `CharacterUnit.initial_energy` 生效）
- `techniques`：秘技编号（暂不建模）

**relics**（list，按 `equip_avatar` 归属角色）：
- `relic_id` 末位 = 部位（1头部 2手部 3躯干 4脚部 5位面球 6连结绳）
- `relic_set_id`：套装编号（**只记录不计算**，套装效果待实现）
- `main_affix_id`：主词条（按部位映射，见 10.3）
- `sub_affixes`：副词条 `[{count, step, sub_affix_id}]`

**lightcones**（list）：
- `item_id`：与 nanoka 光锥编号对应（如 23056 = 一场谎言的终幕），基础属性经 nanoka 详情拉取
- `level`/`promotion`：固定 80/6；`rank`：叠影数（只记录）

### 10.2 副词条数值表

每个 `sub_affix_id` 有 3 个强化档位；**最终值 = 最低档×count + (第二档-最低档)×step**。

| id | 属性 | 三档数值 |
|---|---|---|
| 1 | 生命值 | 33.870 / 38.104 / 42.338 |
| 2 | 攻击力 | 16.935 / 19.052 / 21.169 |
| 3 | 防御力 | 16.935 / 19.052 / 21.169 |
| 4 | 生命值百分比 | 3.456% / 3.888% / 4.320% |
| 5 | 攻击力百分比 | 3.456% / 3.888% / 4.320% |
| 6 | 防御力百分比 | 4.320% / 4.860% / 5.400% |
| 7 | 速度 | 2 / 2.3 / 2.6 |
| 8 | 暴击率 | 2.592% / 2.916% / 3.240% |
| 9 | 暴击伤害 | 5.184% / 5.832% / 6.480% |
| 10 | 效果命中 | 3.456% / 3.888% / 4.320% |
| 11 | 效果抵抗 | 3.456% / 3.888% / 4.320% |
| 12 | 击破特攻 | 5.184% / 5.832% / 6.480% |

### 10.3 主词条数值表

| 部位 | main_affix_id → 属性 |
|---|---|
| 头部 | 1 生命 705 |
| 手部 | 1 攻击 352 |
| 躯干 | 1 生命%43.2 / 2 攻击%43.2 / 3 防御%54 / 4 暴击率32.4 / 5 暴击伤害64.8 / 6 治疗量加成34.5 / 7 效果命中43.2 |
| 脚部 | 1 生命%43.2 / 2 攻击%43.2 / 3 防御%54 / 4 速度 25 |
| 位面球 | 1 生命%43.2 / 2 攻击%43.2 / 3 防御%54 / 4~10 属性伤害提高 38.8% |
| 连结绳 | 1 击破特攻64.8 / 2 能量恢复效率19.4 / 3 生命%43.2 / 4 攻击%43.2 / 5 防御%54 |

脚部/位面球 id1-3、连结绳 id3-5 与躯干同值（同名主词条数值相同）。
百分比统一存小数（0.432 = 43.2%）。位面球 id4-10 元素顺序（物理/火/冰/雷/风/量子/虚数）**待实测校准**；因 `dmg_bonus` 暂为单值（stats.py TODO），当前全部写入属性增伤。

### 10.4 光锥基础值

光锥详情 stats 为晋升段列表，80 级取 promotion=6 行：`基础 = base + add×79`（与角色公式一致，成长 ×(等级-1)），只含 HP/ATK/DEF。

### 10.5 导入流程

1. 配置页"导入 freesr-data.json" → 选文件
2. 解析 avatars/relics/lightcones（`src/core/freesr.py`，纯逻辑可测）
3. 按 char_id 匹配队伍行；已配置但不在队伍中的角色列表提示
4. 光锥详情经 nanoka 异步拉取（diskcache 缓存）；失败按 0 处理并提示
5. **最终面板**（角色基础 + 光锥基础 + 遗器加成）写入队伍表格，覆盖当前数值；技能等级写入全局 SpinBox；`sp_value` 作为初始能量生效

### 10.6 边界与 TODO

- 空配置（avatar 无 data）跳过；遗器/光锥 equip_avatar 无对应角色丢弃
- 导入时角色详情未加载 → 详情加载完成后自动补填
- 已实现 freesr-data.json 中出现的六套遗器效果（110/115/132/308/319/326）
- TODO：其余遗器套装效果、位面球元素顺序
- 星魂效果与已实现的三张光锥叠影效果（23056/23059/23038）已接入模拟器

---

## 11. 真实角色技能模块

真实角色（不死途 1504、千冶•刃 1507）通过**事件钩子模块**接入模拟器，新角色无需改动模拟器核心。

### 11.1 模块框架（src/core/characters/）

- `base.py`：`CharacterModule` 基类，子类定义 `CHAR_ID`（nanoka 角色 ID）与纯数据状态字段（snapshot deepcopy 安全，不持有 sim/char 引用）
- `__init__.py`：`@register` 装饰器注册（char_id → 模块类），`get_module_cls(char_id)` 查表；`setup()` 时按 `CharacterUnit.char_id` 挂载并触发 `on_battle_start`
- `ashveil.py`：不死途（饲饵/充能/婪酣/天赋追击/终结技链/秘技进战）
- `mortenax.py`：千冶•刃（生命上限倍率/无量忿怒结界/煞火缠身/天赋充能/倒计时）

**事件钩子**（模拟器分发，模块按需覆盖）：

| 钩子 | 时机 | 用途 |
|---|---|---|
| `on_battle_start(sim, char)` | 战斗开始 | 初始化状态、秘技进战 |
| `on_resolve_skill(sim, char, skill_type, skill)` | 回合行动/插队终结技前 | **替换**技能（无量忿怒下普攻/终结技强化版）或**返回 None 拒绝**施放（千冶战技限制）；返回原 skill 表示不参与 |
| `on_skill_cast(sim, char, skill, action, target, log)` | 伤害结算前 | 标记目标、消耗生命、清空无伤害技能的误解析 effects |
| `on_attack_hit(sim, attacker, skill, target, effect, damage, log, action_token)` | 每次命中后 | 天赋触发（千冶充能/不死途追击） |
| `on_skill_end(sim, char, skill, action, target, log)` | 技能结算后 | 追加伤害段、终结技追击链（日志顺序在此正确） |
| `on_countdown(sim, char, log)` | 行动条倒计时行动 | 结界解除等 |
| `on_enemy_act(sim, enemy, log)` | 怪物行动后 | 敌方回合计时（煞火缠身剩余回合 -1） |
| `skill_deny_reason(sim, char) → str\|None` | UI 交互按战技前 | 战技被拒原因（未开结界/生命≤1），弹窗提示 |

### 11.2 行动令牌（按"行动"粒度去重的核心）

`BattleSimulator.action_token`：**每次技能施放（`_resolve_skill`）递增**。一次行动内的主伤害 + 模块 `on_skill_end` 追击链共享同一令牌；模块可用 `sim.begin_new_action()` 主动开启新行动（不死途终结技追击链、天赋追击、千冶追加战技均独立）。

**冻结令牌**：`on_attack_hit` 分发时模拟器传入**冻结的 `action_token`**（伤害循环开始时快照）——分发循环内其他模块 `begin_new_action()` 修改全局令牌不影响本次命中的去重键。模块按"行动"去重必须用该参数（读 `sim.action_token` 会在连锁（令牌交替 T/T+1/T+2）时误判）。

去重示例：
- 千冶天赋充能"每次攻击 +1"：战技 5 段（同一令牌）计 1 点；不死途终结技本体 1 点 + 追击链整体 1 点；不死途天赋追击、千冶追加战技各 1 点
- 不死途天赋"受到攻击回 8 能量"：按行动计（战技 5 段命中只回一次），充能不足时回能照常、追击不触发

### 11.3 行动条倒计时

`sim.add_countdown(owner_char, speed=, name=)`：在行动序列插入倒计时单位（如千冶无量忿怒倒计时，固定 70 速），同角色已有倒计时时刷新位置。倒计时行动时触发所属模块 `on_countdown`（结界解除）后从队列移除。`sim.is_auto_unit(actor)` 统一判定怪物/阿哈/倒计时为自动行动单位（UI 交互空格自动执行）。

### 11.4 最小 HP 模型与敌方易伤

- `CharacterUnit.current_hp`：当前生命值（模块在 `on_battle_start` 初始化为面板生命上限；生命消耗/回复由模块管理，上限取 `final_stats().hp`）。千冶战技/终结技消耗生命，不足降至 1；生命 ≤1 无法施放战技
- `EnemyState.vulnerability`：敌方易伤（模块增量式累加/撤销，如千冶【煞火缠身】受伤 +30%）；`def_reduce` 同样改为各模块维护贡献值（不死途增量式，与煞火缠身共存）

### 11.5 技能解析细节

- `skill.py`：`bp_need=-1` 语义——普攻 = 回复（数量 `bp_add`）；**非普攻 = 不消耗**（千冶战技"不消耗战技点"、终结技不回复 SP）
- 千冶生命倍率伤害：`on_skill_cast` 把 effects[0].base_stat 修正为 `"hp"`（parse 默认 atk）；150703 终结技本体无伤害（#1 是生命消耗），effects 清空防误伤
- 技能无效行动（`on_resolve_skill` 返回 None）：回合照常消耗，返回"无法施放（技能受限）"日志（与战斗结束区分，UI 不误判）

### 11.6 双技能类型标记

`DamageRecord.secondary_skill_type`：同时属于两种技能类型的伤害（主类型 + 第二种）。千冶天赋触发的战技"本次战技视为追加攻击"：每段伤害 `skill_type=FOLLOW_UP` + `secondary_skill_type=SKILL`——统计/未来 buff 判定（战技伤害提高 / 追加攻击伤害提高）两者兼顾。`deal_damage(..., secondary_skill_type=...)` 透传。

### 11.7 千冶•刃机制要点（mortenax.py）

- 伤害全部基于**生命上限**（base_stat="hp"）；普攻 25%（强化 50%）、战技 36%+4×12%、强化终结技 210%
- 战技/终结技消耗生命（10%/20%），不足降至 1；生命 ≤1 或未开结界无法施放战技（`on_resolve_skill` 拒绝 + UI 弹窗）
- 终结技开结界：全体【煞火缠身】（减防 20% + 易伤 30%，2 个敌方回合，`on_enemy_act` 计时）、无量忿怒 buff（暴击率 +20%、暴伤 +30%）、70 速倒计时（回合开始结界解除）
- 无量忿怒下：普攻换 150708（50%）、终结技换 150714（210%，耗能 160）、战技解锁
- 天赋充能：结界期间每次攻击（按行动，含自身）+1，封顶 9；达 9 且生命 >1 → 消耗充能、回能量（等级参数，10 级 = 25）、**额外施放战技**（独立行动、双技能类型标记、战技回能 30）；生命 ≤1 时充能保留
- 【百炼骨】行迹：战斗开始/结界解除时能量不足 75% 恢复至 75%（比例从行迹 param_list[0] 读取）
- 倒计时 70 速（142.86 AV 后行动）：100 速千冶开结界后通常轮不到结界内行动（游戏真实表现）

### 11.8 缇宝机制要点（tribbie.py）

- 同谐/量子，能量上限 120；伤害全部基于**生命上限**（base_stat="hp"）
- 【神启】：战技/秘技进战获得，持续 3 回合（自身回合开始 -1）；我方全体**全属性抗性穿透**提高（12%→30%）
  - 抗性穿透新乘区：`FinalStats.res_pen` → `DamageContext.res_pen` → `calc_resistance` 最终抗性 = 抗性 − 穿透（钳制 [-100%, 90%]）
  - 神启以 buff（`stat="res_pen"`，TURNS_SELF_START）加到全体我方角色，`source_unit`=缇宝 → 只有缇宝回合开始时 tick 扣减
- 终结技开结界：全体 #1（15%）生命上限量子伤害 + 敌方受伤提高 #2（15%，vulnerability 增量式）+ 持续 #4（2）回合（缇宝回合开始 -1，`on_turn_start` 钩子扣减，归零解除并撤销易伤/A2）
- 结界附加伤害：我方目标攻击（**按行动计**，含缇宝自己）后，对被攻击目标造成 #3（6%）生命上限量子附加伤害；**并入触发它的那次行动**（不单独成行动、不建独立日志），攻击类型为【附加伤害】`SkillType.ADDED`（不属于普攻/战技/终结技/追加攻击），伤害来源为缇宝
- **附加伤害 ≠ 攻击**：`deal_damage` 的 `is_attack=False` 分支不触发 `on_attack_hit` 钩子与 NEXT_ATTACK buff tick——不死途天赋、千冶充能等"攻击后"系效果不受附加伤害影响（曾因不死途追击 `begin_new_action` 改全局令牌导致缇宝终结技后不死途误触发 2 次追击，已回归测试覆盖）；目标简化取触发命中的目标（无敌方 HP 模型，"生命最高目标" TODO）
- 天赋：我方其他角色施放终结技后，缇宝全体追加攻击 #1（9%）生命上限量子伤害（独立行动 + 追击回能 5）；每角色最多 1 次，缇宝终结技重置
- 行迹（point_type=3，按名称前缀匹配读取，UI 通路走兜底常量）：
  - A2【长翅膀的玻璃球】：结界期间缇宝生命上限 = 我方全体生命上限之和的 9%（hp_flat buff，结界开启时计算一次）
  - A4【岔路旁的小石子】：战斗开始回 30 能量；我方**其他**目标攻击（按行动）后回 1.5 能量
  - A6【城墙外的羊羔儿】：天赋追击后缇宝增伤 72%/层，最多 3 层，持续 3 回合（dmg_bonus buff，STACK_LIMIT_N）
- 新钩子：`on_turn_start`（角色回合开始，模块做自身回合计时）在 `_character_act` 的 buff tick 后分发

### 11.9 UI 集成

- 交互模式：`is_auto_unit` 统一处理怪物/阿哈/倒计时自动行动；战技前查 `skill_deny_reason` 弹窗
- 充能徽章：模块类属性 `CHARGE_STYLE`（"dots" 圆点 / "text" 文字）与 `CHARGE_MAX`——不死途 3 紫色圆点，千冶红色文字 "x/9"（缇宝无资源徽章）
- 甘特图：倒计时行动点（青灰）、行高/间距可调（LANE_HEIGHT/LANE_GAP）、刻度与行动点裁剪到甘特图区域（标签/标题不裁剪）、同 AV 事件纵向排列

### 11.10 未建模（TODO，无我方受击/多目标模型）

- 千冶：致命攻击免死（结界内受致命伤解除结界回 50% 生命）、嘲讽（普攻/秘技）、秘技减伤 90%、战技额外段/强化终结技削韧数据
- 不死途：终结技强化追击"致命攻击转移"、战技"生命最低敌人"选取
- 缇宝：普攻相邻目标伤害（#2，需敌人位置模型）、附加伤害"生命最高目标"选取（无敌方 HP 模型，暂取触发命中目标）、结界期间生命上限随我方 HP 变化动态重算、追击前目标被消灭则转移新入场敌人、终结技/追击削韧（show_stance_list 第二项语义待勘探）
- 通用：敌方 HP/死亡模型、我方受击模型、按技能类型的增伤 buff（dmg_bonus 单值）

---

## 附录 A：参考资料

- **数据源**：https://hsr.nanoka.cc
- **参考项目**：https://github.com/d4rkOfficial/wuwa-afyg-tool
- **PySide6 文档**：https://doc.qt.io/qtforpython-6/
- **pydantic v2 文档**：https://docs.pydantic.dev/latest/
- **星铁机制 Wiki**：https://honkai-star-rail.fandom.com/wiki/Stats

## 附录 B：开发阶段里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 | 数据勘探 | ✅ 已完成 |
| 1 | API 数据层 | ⬜ 待开始 |
| 2 | 业务核心建模 | ⬜ 待开始 |
| 3 | 界面骨架 | ⬜ 待开始 |
| 4 | 排轴与模拟 | ⬜ 待开始 |
| 5 | 结果与打包 | ⬜ 待开始 |
